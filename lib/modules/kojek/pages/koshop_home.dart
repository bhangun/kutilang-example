import 'package:flutter/material.dart';

class KoShopHome extends StatelessWidget {
  const KoShopHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(''),
    );
  }
}