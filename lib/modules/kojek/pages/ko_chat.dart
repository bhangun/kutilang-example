import 'package:flutter/material.dart';

class KoChat extends StatelessWidget {
  const KoChat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(''),
    );
  }
}