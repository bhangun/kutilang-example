import 'package:flutter/material.dart';

class KoMartHome extends StatelessWidget {
  const KoMartHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(''),
    );
  }
}