import 'package:flutter/material.dart';

class KoSendHome extends StatelessWidget {
  const KoSendHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(''),
    );
  }
}